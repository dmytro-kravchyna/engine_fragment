#include "Schema/index_generated.h"

// Minimal translation unit so the 'fragments' library has at least one
// compilation unit. The generated header contains all necessary templates and
// symbols; this file intentionally does very little.
namespace fragments {
void ensure_link() {}
} // namespace fragments
